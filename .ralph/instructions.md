# Rules

- Create a changeset using `bun changeset` for each change BEFORE you commit
- Verify implementation by running `bun check` before committing
- Follow the existing code style and conventions
