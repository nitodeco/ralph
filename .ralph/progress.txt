## Session Progress

### Task: Add max runtime limit configuration
Status: COMPLETED

Implementation details:
- Added `maxRuntimeMs` to RalphConfig type (already existed)
- Added `--max-runtime` CLI flag parsing (already existed)
- Implemented runtime tracking in iterationStore with `setMaxRuntimeMs`, `setStartTime`, `getTimeRemaining`, and `isMaxRuntimeReached` methods
- Added `onMaxRuntime` callback to iteration callbacks
- Check runtime limit before starting new iteration in the `next()` function
- Added "max_runtime" app state for when runtime limit is reached
- Display time remaining in StatusBar when limit is set
- Updated orchestrator to handle max runtime configuration and callback
- Set start time when starting/resuming sessions to track runtime
