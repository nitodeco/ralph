---
"ralph-cli": minor
---

Add configuration validation and 'ralph config' command

- Add comprehensive config validation with clear error messages
- Add CONFIG_DEFAULTS with sensible defaults for all settings
- Add 'ralph config' command to view current configuration
- Show global and project config paths
- Display validation warnings (e.g., stuck threshold > timeout)
- Support --json flag for machine-readable output

