---
"ralph": patch
---

Fixed model resolution and `/model` selection behavior so all supported agents show a resolved model in the UI and can set models consistently when discovery data is fallback-only.
