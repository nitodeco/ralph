---
"ralph": patch
---

Add task slash commands in terminal UI. Users can now use `/task done <id>`, `/task undone <id>`, `/task current`, and `/task list` to manage tasks directly from the command input without navigating to the tasks view.
