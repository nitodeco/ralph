---
"ralph": patch
---

Add Linux support to the update system. Linux binaries (arm64 and x64) are now included in releases and the self-update system can download and install them on Linux platforms.
